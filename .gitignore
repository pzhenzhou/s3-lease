# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
bin/*
# `go build` inside the deploy module defaults to this name; the orchestrator
# is built to bin/lake-deploy by `make deploy-tool`.
/deploy/deploy
Dockerfile.cross
# Test binary, built with `go test -c`
*.test
.cursorrules
# Output of the go coverage tool, specifically when used with LiteIDE
*.out
# CLAUDE.md
# AGENTS.md
# Go workspace file
go.work

# Kubernetes Generated files - skip generated files, except for vendored files
!vendor/**/zz_generated.*

# editor and IDE paraphernalia
.idea/
.cursor/
.github/
.vscode
.claude
*.swp
*.swo
*~
tmp/
temp/
*.log
GEMINI.md
logs/*
log/*
tests/foundry-test
/migration/bin/

# Local Flight SQL server state (make serve-local)
.semcache-local/
*.duckdb
*.duckdb.wal
*.duckdb.dev-bootstrap-*

# Flight SQL interoperability build outputs
__pycache__/
target/
